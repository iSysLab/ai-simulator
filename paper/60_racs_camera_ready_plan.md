# RACS 2026 camera-ready 대응 설계 v2 — 지적마다 새 증거를 추가하는 계획

작성 2026-09-15 (v2). 대상: ACM RACS 2026 paper 39, 리뷰어 1 지적 12개 + 리뷰어 2.

**v1에서 고친 점.** v1은 R1-1·2·5·7·10과 R2에서 "서술 보완·톤 조정"에 기대는 부분이 있었다. v2는 항목마다 **새로 추가하는 실험·코드·표**를 먼저 정의하고, 문장 수정은 그 결과의 귀결로만 둔다. 재측정도 "GAN만"이 아니라 GPU 두 백엔드 전체로 넓혔다(비용이 감당 가능함을 확인).

## 0. 전제와 자원

- 마감 **2026-10-07**(등록 + camera-ready), 회의 09-21. 영문 제출본 확보가 1순위(저장소에 없음, 국문 v4에는 §4.4 LOFO/LOBO가 있는데 리뷰어는 없다고 함).
- 측정 비용(원시 데이터의 평균 실행시간 합 × 10회):

| 백엔드 | 전체 158~160구성 × 10회 | GAN 8구성만 | 기기 |
|---|---:|---:|---|
| Desktop CPU | 27.2 h | 0.31 h | 이 PC |
| CUDA | **2.4 h** | 0.09 h | 이 PC, RTX 4060 Ti |
| Mac CPU | 101.8 h | 0.19 h | 맥북 M4 |
| MPS | **9.1 h** | 0.11 h | 맥북 M4, 야간 1회 |

  → GPU 두 백엔드는 **전체 재측정이 가능**하다. CPU 두 백엔드는 불가하므로 그대로 둔다.
- 실행 환경: 이 PC `.venv`(Python 3.11.9, torch 2.7.1+cu118, scikit-learn 1.8.0, xgboost 3.2.0). 파이프라인: `enrich_oplevel.py` → `export_onnx_configs.py` → `eval_fill_v4i.py` → `eval_fill_v4j.py` → (신설) `eval_fill_v4k.py`. 앞의 두 산출물은 gitignored라 재생성이 필요하다.

## 1. 지적별 보충 계획 (요약)

| # | 새로 추가하는 증거 | 코드 산출물 | 논문 산출물 | 측정 |
|---|---|---|---|---|
| R1-1 | 구간·백엔드별 오차 표, 오버헤드 피처로 <10ms 오차 개선 시도, 잡음 하한, 보정 산점도 | v4k `errors_by_regime`, `overhead_features`; `make_scatter.py` 갱신 | 오차 표(잡음 열 포함) + 그림 | 없음 |
| R1-2 | 검증 프로토콜 사다리 7단계 | v4k `protocol_ladder` | 표 7을 7행으로 교체(±시드) | 없음 |
| R1-3 | 백엔드별 하드웨어 기술자 값 | v4k `backend_values` | 표 2에 열 5개 | 없음 |
| R1-4 | LOFO 복원, 사다리에 통합 | (기존 `lofo_no_family_id`) | §4.2·§4.4 | 없음 |
| R1-5 | 라벨 1개 vs 기술자 30개, LOBO 기준선, **외부 5번째 백엔드 시험** | v4k `label_vs_descriptors`, `external_backend_test`; `run_benchmark.py --subset` | 표 7 행 2개 + 외부 백엔드 표 | 외부 기기 1~2h |
| R1-6 | 통합 XGBoost permutation·gain 중요도 | v4k `unified_importance` | 표 8·9 교체 | 없음 |
| R1-7 | 실제 배치(64/1000) 기준 프록시 + arithmetic intensity, 대칭 그룹 절제, 단독 피처 기준선 | `onnx_oplevel.py` 확장, v4k `proxy_batch_scaled`, `symmetric_ablation`, `single_feature_baselines` | 표 9 재구성, §3.3 정의 갱신 | 없음 |
| R1-8 | GB·Ridge 동일 그리드 nested CV | v4k `tuned_comparison` | 표 5 갱신 | 없음 |
| R1-9 | 4자리 + 부트스트랩 CI | v4k `precision_check` | 각주 | 없음 |
| R1-10 | GPU 두 백엔드 전체 재측정(통일 워밍업, 반복별 원시값, 피크 메모리) | 러너 4개 파일 | §3.1 프로토콜, 표 3 재계산 | CUDA 2.4h + MPS 9.1h |
| R1-11 | 셀별 CV, 잡음 하한 대 예측 오차, 2σ 이내 비율 | v4k `measurement_noise`, `load_clean` std 풀링 | 오차 표 열 | R1-10과 공유 |
| R1-12 | 환경 자동 기록, 데스크톱 버전(확보) | `software_env.py`, `check_env.py --json` | §3.1 | 맥 1회 |
| R2 | 배치 스윕(CUDA), 피크 메모리(GPU), 외부 백엔드, Limitations | `--batch-sizes` 옵션 | 배치 표 1개, 메모리 결과 | 배치 스윕 0.5h |

## 2. 항목별 상세

### R1-1 헤드라인 지표와 개별 예측 정확도의 괴리

**추가 증거**
1. 오차 표: 백엔드 × {전체, ≥10ms, <10ms, GAN} × {MAPE, MdAPE, ±20%}. 같은 칸에 측정 잡음 CV 중앙값(R1-11)을 넣어 "잡음 하한 대비 예측 오차"가 한 표에서 읽히게 한다.
2. <10ms 오차 개선 시도. (a) 오버헤드 피처 추가: `num_batches_train`(MNIST 60000/64=938, CIFAR 50000/64=782), `num_batches_infer`(10000/1000=10), `ops_x_batches = num_ops × num_batches`, `log_num_ops`. (b) 소형 구간 전문 모델(학습 셀 <50ms)과 통합 모델의 <10ms MAPE 비교. (c) 목적함수 `reg:pseudohubererror` 비교. 개선되면 통합 모델에 채택하고, 아니면 "정적 피처로는 커널 실행 오버헤드가 지배하는 구간을 설명하지 못한다"를 수치로 서술한다.
3. 보정 산점도(실측 vs 예측, 로그-로그, 백엔드 색, <10ms 표시). `make_scatter.py`를 nested CV OOF 기준으로 갱신해 그림 1장 추가.

**논문**: 초록·결론은 R²(log)·MAPE·±20% 3종 병기. §4.1에 오차 표와 그림. 문장은 이 결과의 요약이다.
**판정 기준**: <10ms MAPE(현재 54.1%)가 시드 5개 평균에서 내려가는지와 무관하게 표·그림은 들어간다.

### R1-2 어떤 분할이 실제 구속 조건인가

**추가 증거** — 검증 프로토콜 사다리(모두 XGBoost nested CV, R²(log)와 MAPE(≥10ms)):
1. 무작위 5-겹 (시드 10개 평균±표준편차)
2. 구성 단위 GroupKFold (시드 10개, `shuffle=True`, sklearn 1.8 지원 확인)
3. 폭 수준 제외: 계열마다 한 폭 값(예: CNN f64, ANN h256)의 구성을 모두 홀드아웃. 이웃 폭 사이 보간이 아닌 폭 축 외삽.
4. 깊이 수준 제외: 같은 방식으로 깊이 축.
5. 최대 사분위 제외: 계열별 파라미터 수 상위 25% 구성 홀드아웃. "더 큰 후보가 너무 느린가"라는 NAS 질문 그대로.
6. 계열 제외(LOFO, 기존)
7. 백엔드 제외(LOBO, 기존)

**코드**: v4k `protocol_ladder`. 3~5단계는 `benchmark/configs/generator.py`의 폭·깊이 키(`hidden_size`, `num_filters`, `embed_dim`, `num_layers`, `num_blocks` 등)로 그룹 라벨을 만들어 GroupKFold 또는 LeaveOneGroupOut.
**논문**: 표 7을 이 사다리 7행으로 교체. §4.2는 "정확도가 어느 단계에서 꺾이는지"를 서술하고, 무작위 대 구성 단위의 0.003 차이는 시드 표준편차와 함께 한 줄로.
**판정 기준**: R²(log)가 처음 크게 떨어지는 단계가 "구속 조건"이고, 그 단계를 기준으로 적용 범위를 명시한다.

### R1-3 백엔드 구분 피처

**추가 증거**: 하드웨어 수치 30개의 백엔드별 값 표(백엔드 안에서 상수임을 함께 확인). 대표 5개(`device_type_encoded`, `cpu_cores`, `ram_total_gb`, `gpu_cores`, `tflops_fp32`)를 표 2에 열로 추가. 확인된 값: Mac CPU는 `device_type_encoded=0, gpu_cores=0, gpu_memory_gb=0`, MPS는 `2, 10, 18`. Desktop CPU와 Mac CPU는 `cpu_cores` 16 대 10, `ram_total_gb` 31.1 대 24.
**코드**: v4k `backend_values`. Mac `cpu_freq_ghz=0` 같은 미수집 필드는 "–"로 표기하고 상수 처리임을 본문에 적는다.

### R1-4 LOFO

**추가 증거**: 기존 LOFO(계열 식별자 제거판, MAPE 포함)를 사다리 6단계로 통합. 영문본에 없으면 복원하고, 서론 기여 목록에 NAS 시나리오 검증으로 명시.

### R1-5 하드웨어 피처가 "정체성" 이상의 정보를 갖는가

**추가 증거**
1. 라벨 1개 vs 기술자 30개(구성 단위 nested CV): (a) 전체 피처, (b) 기술자 30개 제거, (c) 제거 + `_backend_id` 1개, (d) 제거 + `device_type_encoded`만(두 CPU 미구분). 기대는 (c)≈(a)이고, 그 결과 자체가 표에 들어간다.
2. LOBO 기준선: 라벨 모델은 미지 백엔드에 정의되지 않으므로 "다른 세 백엔드 예측의 평균"과 "같은 기기 종류의 최근접 백엔드 예측"을 기준선으로 두고 기술자 모델의 LOBO와 비교. 기술자 모델이 기준선을 이기는 백엔드(CPU 계열)와 못 이기는 백엔드(CUDA)를 구분해 보고한다.
3. **외부 5번째 백엔드 시험**(기기가 있으면 반드시): 연구실의 다른 기기 1대(인텔/AMD 노트북 CPU, 다른 NVIDIA GPU, 또는 M1 Air를 현재 프로토콜로)에서 부분집합을 측정하고, 4백엔드로 학습한 모델로 예측. 부분집합은 계열별 균등 40~60구성 × 3회. 비용은 소형 40구성이면 수 분, 중형 포함 60구성이면 1~2h.

**코드**: `run_benchmark.py --subset stratified:60 --repeats 3` 옵션(계열별 균등 추출, 시드 고정, 결과 `results/external_<host>.json`); v4k `label_vs_descriptors`, `external_backend_test`(기술자 모델 vs 최근접 백엔드 기준선, R²(log)·MAPE·순위 상관).
**논문**: "explicit hardware features are crucial"을 1~3의 결과로 교체. 외부 백엔드 결과가 있으면 §4.4에 1행짜리 표. 이것이 R2의 "미지 하드웨어 일반화 부족"에 대한 실측 답이 된다.

### R1-6 중요도의 출처 모델

**추가 증거**: 통합 XGBoost(구성 단위 nested CV의 각 외부 폴드 best 모델)의 홀드아웃 permutation importance(n_repeats=20). 단일 피처(`total_op_memory_write`, `flops`)와 그룹 공동 셔플(MEM/FLOPs). 폴드 평균±표준편차, gain 중요도 순위 병기. 백엔드별 RF는 보조 한 줄.
**코드**: v4k `unified_importance`.
**논문**: 표 8·9의 주 결과 교체, 서론 기여문 정정.

### R1-7 메모리 프록시: 실제 배치 기준 재계산과 고유성

**추가 증거**
1. 프록시를 실제 배치로 재계산. `decompose_onnx`는 op마다 `in_elems`(활성화)·`w_params`(가중치)·`out_elems`를 이미 분리해 계산한다(`memory_read = in_elems·4 + w_params·4`, `memory_write = out_elems·4`, 배치 1). B=64(학습)·B=1000(추론)에 대해 `read_B = 4·(in_elems·B + w_params)`, `write_B = 4·out_elems·B`, `arith_intensity_B = flops·B / (read_B + write_B)`를 op별로 합산해 피처 6개를 추가한다. 학습 모델에는 B=64, 추론 모델에는 B=1000 값을 입력.
2. 대칭 그룹 절제(재학습): 전체 / −MEM / −FLOPs / −둘 다. 그룹 크기 비대칭 문제가 없는 비교.
3. 단독 피처 기준선: `mem_write_B` 단독, `flops` 단독(기존 0.558/0.468), `arith_intensity_B` 단독, `mem_write_B + backend_id`.
4. 상관 보고: 로그 공간 Spearman(mem_write, flops), 전체와 계열별.

**코드**: `benchmark/features/onnx_oplevel.py`에 `batch_size` 인자와 op별 원소 수 반환; `merged_schema.py`에 열 6개; v4k `proxy_batch_scaled`, `symmetric_ablation`, `single_feature_baselines`.
**논문**: 표 9를 "배치 기준 프록시 포함 중요도 + 대칭 절제 + 단독 기준선"으로 재구성. §3.3 프록시 정의를 배치 기준으로 갱신. 주장은 결과가 지지하는 만큼만.
**판정 기준**: 배치 기준에서도 mem_write가 flops를 앞서고 arithmetic intensity가 상위면 Roofline 해석 유지, 아니면 "일관되게 선택되는 인자"로 한정.

### R1-8 튜닝 공정성

**추가 증거**: GB에 XGBoost와 같은 그리드 `{n_estimators [200,400], max_depth [3,6], learning_rate 0.1}`로 nested CV. LR은 Ridge(alpha {0.1, 1, 10}) nested CV로 교체. 표 5 재산출.
**코드**: v4k `tuned_comparison`.

### R1-9 표 값 일치

**추가 증거**: 전체·백엔드별 R²(log)를 4자리와 부트스트랩 95% CI(1000회)로. 코드상 백엔드별 R²는 부분집합 자체 평균 기준(`r2_score(y0[bks==b], oof[bks==b])`)이고 JSON 값도 실제로 0.988/0.989로 같다. 각주 한 줄.

### R1-10 워밍업 프로토콜 통일 — GPU 전체 재측정

**추가 증거**: CUDA 158구성(2.4h, 이 PC)과 MPS 158구성(9.1h, 맥 야간)을 새 프로토콜로 전체 재측정. 새 프로토콜은 모든 계열·백엔드에서 실제 학습 배치 1개로 학습 스텝 1회 + 평가 순전파 1회 워밍업(GAN은 D+G 스텝 1회), 반복별 원시값 저장, 피크 메모리 기록(`torch.cuda.max_memory_allocated`, `torch.mps.driver_allocated_memory`).
**코드**:
- `benchmark/runner/device.py`: `warmup(device, model_fn, train_step_fn, eval_batch)`. 현재는 배치 1 no_grad 순전파 1회라 역전파 커널은 데워지지 않는다.
- `benchmark/runner/experiment.py`: `train_times`, `infer_times`, `peak_mem_mb` 저장, `discard_first` 옵션, std의 ddof 명시.
- `run_benchmark.py`: `if not is_gan:` 분기 제거, `--subset`, `--batch-sizes` 옵션.
- `benchmark/results/io.py`: 실행 헤더(시작 시각, 환경).
**병합**: `load_clean()`에 GPU 셀 override. CPU 셀은 그대로 두되 첫 실행의 oneDNN 프리미티브 생성 비용은 std로 상한 제시(10회 중 1회 δ면 평균 편향 ≤ 0.33·std).
**논문**: §3.1 프로토콜 서술을 새 프로토콜로, 표 3(플랫폼 비율) 재계산, 재측정 전후 GAN 셀 변화량 한 문장.
**대안(맥 불가 시)**: CUDA만 전체, MPS는 GAN 8구성만(0.11h).

### R1-11 측정 잡음

**추가 증거**: CPU 셀은 기존 `std_train`·`std_infer`(10회, ddof=0)로, GPU 셀은 재측정 원시값으로. 백엔드 × 타깃 CV 중앙값·p90, <10ms/≥10ms 분리, |예측−실측| ≤ 2σ인 셀 비율. 오차 표(R1-1)에 열로 통합.
예비값(원시 698행): 학습 CV 중앙값 Desktop CPU 1.2%, CUDA 2.1%, Mac CPU 0.6%, MPS 0.6%. 추론 <10ms 셀 CV 중앙값 3.9%, p90 27.9%. ≥10ms는 1.1%, 6.5%.
**코드**: `load_clean` std 풀링 수정(중복 48구성의 std가 첫 행 값으로 남는 문제) + v4k `measurement_noise`.

### R1-12 환경 기록

**추가 증거**: 데스크톱 버전 확보(Python 3.11.9, PyTorch 2.7.1+cu118, CUDA 11.8, cuDNN 9.1.0, oneDNN 3.7.1 + MKL, Windows 11 26200, 드라이버 610.62; torch 설치 2026-01-19로 측정 이전). 맥은 `check_env.py --json` 1회. 재측정 결과 파일에는 자동 기록.
**코드**: `benchmark/platform/software_env.py`, `check_env.py --json`, 결과 행 `sw_*` 필드(문자열, `STRING_FEATURES`에 추가).

### R2 고정 배치·피크 메모리·미지 하드웨어

**추가 증거**
1. 배치 스윕(CUDA, 이 PC): 계열별 균등 30구성 × 학습 배치 {32, 64, 128, 256} × 3회, 약 0.5h. `batch_size`가 변하는 피처가 된 모델의 배치 외삽 성능({32,64,128}로 학습해 256 예측) 표 1개.
2. 피크 메모리: R1-10 재측정에서 GPU 셀의 실측 피크 메모리 확보. "파라미터 메모리로 정의한 타깃" 대신 실측값 예측 결과 한 문장 또는 표 1행.
3. 미지 하드웨어: R1-5의 외부 백엔드 시험.
4. Limitations 소절: 위 실험이 다루지 못한 범위(프로토타입 규모, CPU 배치 스윕 없음)만 남긴다.

## 3. 실행 순서와 일정

- **1주차(~9/21)**: 영문본 확보. 파이프라인 재현(enrich·ONNX 재생성 → v4j 수치 일치 확인). 러너 수정 후 CUDA 전체 재측정 시작(2.4h). v4k에 사다리·라벨 대 기술자·통합 중요도·튜닝·정밀도·잡음 구현 → 월요일 1차 수치.
- **2주차(9/22~28)**: 맥 MPS 재측정(야간), 배치 스윕(0.5h), 외부 백엔드 부분집합(기기 확보 시), 배치 기준 프록시 확장·재분석, 오버헤드 피처 실험, 그림 갱신.
- **3주차(9/29~10/3)**: 영문 원고 표·그림·본문 반영, 교수님 검토.
- **10/4~7**: 양식·등록.

## 4. 월요일 결정 사항

1. 맥북 야간 MPS 전체 재측정 가능 여부(9.1h).
2. 외부 5번째 백엔드로 쓸 기기(연구실 노트북, 다른 GPU, M1 Air).
3. 배치 스윕·피크 메모리 결과를 본문에 넣을지, 분량상 한 문장으로 갈지.
4. 영문본 §4.4 포함 여부와 페이지 여유.
5. **데스크톱 세션 드리프트 대응(§5·§6)**: 소형 3구성 프로브 후 CUDA만 재측정할지, 데스크톱 CPU까지 27h 재측정할지. 맥은 MPS가 v1과 일치해 결정 불필요(§6).

## 5. 2026-09-15 CUDA 재측정 결과와 세션 드리프트 (실측 후 추가)

CUDA 160구성을 v2 프로토콜로 재측정했다(`results/remeasure_cuda_v2.json`, 15:55~18:28). 원래 3월 데이터(v1)와 비교한 결과:

| 계열 | v2/v1 학습시간 중앙값 | v2/v1 추론시간 중앙값 |
|---|---:|---:|
| ANN | 1.22 | 1.36 |
| CNN | 1.00 | 0.88 |
| ResNet | 1.10 | 1.00 |
| MobileNet | 1.10 | 1.00 |
| ViT | 1.12 | 1.01 |
| GAN | 1.11 | 1.04 |

- **느려진 정도가 모델 크기에 반비례한다.** v1 학습시간 1초 미만 구성은 +20%, 1~3초 +24%, 3~10초 +10%, 10초 이상은 0%. ANN에서는 층 수가 늘수록 비가 커진다(1층 +12% → 8층 +29%). 즉 GPU 연산이 아니라 **커널 실행 1회당 지연**이 3월보다 커졌다.
- **GPU는 스로틀링 없음.** 실행 중 P2, 2670MHz, 52W/160W. 소형 모델 실행 중 SM 클럭은 405~2535MHz를 오가며(중앙값 2115) P0~P8 상태를 순환한다. 커널 사이에 클럭이 내려가는 launch-bound 특성 그대로다.
- **환경 변화 확인:** NVIDIA 드라이버가 2026-06-11에 설치됨(610.62). 3월 측정 때와 드라이버가 다르다. 전원 관리 옵션은 "균형 조정". 백그라운드 CPU 부하는 2% 미만(claude 0.5%, nexonplug 0.4%, presentmon, fvcontainer 등 게임 관련 서비스 상주).
- **CPU 쪽도 드리프트가 있다.** 소형 4구성 프로브(5회): ANN 학습 +5~10%, CNN 학습 0%. GPU보다 작다.
- **GAN 워밍업 지적(R1-10)에 대한 답은 오히려 명확해졌다.** v1에서 워밍업이 없던 GAN의 변화(+11%)가 워밍업이 있던 ResNet·MobileNet·ViT(+10~12%)와 같다. 즉 v1의 GAN 워밍업 부재는 측정값에 영향을 주지 않았고, 공통 요인은 세션 드리프트다. v2 원시값에서 첫 반복/나머지 평균 비는 중앙값 1.00, p90 1.11이다.
- **반복 변동은 v1보다 크다.** CUDA 학습 CV 중앙값 v1 2.1% → v2 5.1%.

## 6. 2026-09-16 MPS 재측정 결과 — 드리프트 원인 확정

맥북(Apple M4, macOS 26.6.2, torch 2.10.0)에서 MPS 160구성을 v2 프로토콜로 재측정했다
(`results/remeasure_mps_v2.json`, 09-15 17:15 ~ 09-16 02:15, 9시간). 환경 기록은 `paper/env_mac.json`.

**무결성**: 160/160 구성, 누락·중복 0, 전 행 `protocol=v2-uniform-warmup`, 반복별 원시값 10개,
피크 메모리(`mps_driver_allocated_memory`, 235~4079MB), 환경·부하 필드 완비. `avg`와 원시값 평균 일치.
측정 중 CPU 부하 중앙 13.7%(최대 19.9%), 29개 구성에서 유휴 대기가 걸렸으나 대기 행과 비대기 행의
v2/v1 비(0.985 대 0.989)와 반복 CV(0.9% 대 0.7%)가 같아 측정에 영향을 주지 않았다.

**핵심: MPS에는 드리프트가 없다.** CUDA와 같은 표로 비교하면 차이가 분명하다.

| v1 학습시간 구간 | MPS v2/v1 | CUDA v2/v1 |
|---|---:|---:|
| 1초 미만 | 0.99 | 1.20 |
| 1~3초 | 0.99 | 1.24 |
| 3~10초 | 1.00 | 1.10 |
| 10초 이상 | 0.97 | 0.99 |

ANN 층수별로도 MPS는 0.92~1.00으로 평탄한 반면 CUDA는 1.13(1층)→1.29(8층)로 증가한다.
계열별 중앙값도 MPS는 학습 0.95~1.01, 추론 0.96~1.01이다.

이로써 세 가지가 확정된다.
1. **v2 프로토콜 자체는 측정값을 바꾸지 않는다.** 통일 워밍업이 느려짐의 원인이었다면 MPS에서도 나타나야 한다.
2. **CUDA 드리프트는 데스크톱 기기 상태 변화다.** 드라이버 교체(2026-06-11), 전원 옵션, 상주 게임 서비스가 후보.
3. **R1-10(GAN 워밍업)은 두 플랫폼에서 독립적으로 해소된다.** MPS에서도 v1에 워밍업이 없던 GAN의
   변화(1.01)가 워밍업이 있던 계열과 같다. 단 `GAN_z64_G128_256` 한 셀만 0.81에 CV 12.3%로 튀므로
   이 셀은 재확인 대상이다.

**표 3(플랫폼 간 상대 학습시간)이 재현된다.** MPS만 v2로 바꿔 계열 평균을 다시 계산하면 논문 값과 반올림 오차 안에서 일치한다.

| 계열 | 논문 표기 | v1 재계산 | v2 MPS 기준 |
|---|---:|---:|---:|
| ANN | 0.5 | 0.48 | 0.48 |
| CNN | 5.1 | 5.07 | 5.28 |
| ResNet | 7.1 | 7.13 | 7.30 |
| MobileNet | 35.0 | 35.03 | 34.63 |
| ViT | 2.7 | 2.69 | 2.74 |
| GAN | 1.7 | 1.70 | 1.71 |

즉 논문의 동기 관찰(MobileNet 35배 가속, ANN 계열 방향 반전)은 다른 세션·더 엄격한 프로토콜에서 재현된다.
이는 리뷰어 2의 재현성 지적에 대한 직접적인 증거다.

**v1 MPS 소형 추론에 냉시작 오염이 있었다.** v2 워밍업이 이를 제거한다.

| 구성 | v1 추론 | v2 추론 |
|---|---|---|
| ANN_h16_l1 | 0.0880 ± 0.1153 s | 0.0061 ± 0.0003 s |
| ANN_h32_l1 | 0.0185 ± 0.0161 s | 0.0064 ± 0.0003 s |
| ANN_h64_l1 | 0.0217 ± 0.0210 s | 0.0080 ± 0.0005 s |

v1에서는 표준편차가 평균보다 컸다. 반복 변동도 개선되어 MPS 학습 CV p90이 8.1%→2.3%,
추론 p90이 12.9%→6.0%로 내려갔다. **이는 R1-1·R1-11에 직접 쓰인다.** 추론 MAPE 54.1%가 집중된
10ms 미만 구간의 일부는 모델 오차가 아니라 v1의 측정 오염이었을 가능성이 크다. v2 데이터로
재평가하면 이 구간의 오차가 실제로 줄어드는지 확인할 수 있다(§3 R1-1의 판정 기준).

한 가지 부작용: 논문 §3.2의 "`ANN_h16_l1`에서는 약 3.55배 느렸다"는 v2 기준으로 2.79배가 된다.
계열 평균(약 2배)은 유지되므로 이 문장의 수치만 갱신하면 된다.

## 7. 2026-09-16 데스크톱 프로브 — 재측정 불가 판정

§6의 권장 순서대로 데스크톱에서 프로브를 돌렸다. 대상은 드리프트가 컸던 3구성과 대조군 1구성,
각 5회 반복(`results/probe_base.json`, `results/probe_hiperf.json`).

| 구성 | v1(3월) | v2(어제) | 프로브 균형 | 프로브 고성능 |
|---|---:|---:|---:|---:|
| ANN_h16_l1 (대조) | 0.529 | 0.517 | 0.569 | 0.513 |
| ANN_h64_l6 | 0.977 | 1.387 | 1.395 | 1.266 |
| ANN_h128_l8 | 1.312 | 1.819 | 1.737 | 1.937 |
| CNN_f8_l1_bn0 | 0.759 | 0.902 | 0.843 | 0.841 |
| **v1 대비 중앙값** | 1.000 | 1.19 | **1.22** | **1.20** |

**전원 옵션은 원인이 아니다.** 고성능으로 바꿔도 v1 대비 중앙값이 1.22에서 1.20으로 거의 그대로다.
오버레이 프로세스(NVIDIA Overlay 5개)는 강제 종료해도 즉시 재생성되고, PresentMon·NexonLauncher·
FvContainer.System은 관리자 권한이 없어 종료되지 않았다.

**진짜 문제는 평균 이동이 아니라 분산 증가다.** 반복별 원시값을 보면 분명하다.

| 구성 | v1 반복 CV | 어제 본측정 CV | 프로브 CV |
|---|---:|---:|---:|
| ANN_h16_l1 | 5.9% | 2.3% | 0.9~1.9% |
| ANN_h64_l6 | 3.0% | 5.1% | 5.0~7.9% |
| ANN_h128_l8 | 2.7% | 10.0% | 2.6% |
| CNN_f8_l1_bn0 | 4.0% | 5.6% | 3.2~6.0% |

`ANN_h128_l8`의 어제 10회 원시값은 2.09 → 1.61 → 1.98로 **한 번의 실행 안에서** 오르내린다.
프로브 두 번의 평균도 1.737과 1.937로 12% 차이가 난다(수 분 간격, 같은 설정). 즉 이 기기는 현재
launch-bound 워크로드에 대해 **재현 가능한 측정 환경이 아니다**. 3월에는 CV 중앙값 2.1%로 안정적이었다.

**판정: 관리자 권한 없이는 CUDA 재측정으로 3월 수준의 데이터를 얻을 수 없다.**

**따라서 §5의 (C)를 채택한다.**
- 논문 본문 수치는 **v1 유지**(데스크톱 CPU·CUDA가 같은 세션, CV 2~4%로 안정).
- CUDA v2는 심사 답변 전용으로 쓴다: R1-10(GAN 워밍업 무영향), R1-11(반복별 원시값 분포),
  R1-12(환경 기록), 그리고 "측정 환경이 바뀌면 launch-bound 구성이 20~30% 흔들린다"는 한계 서술.
- MPS v2는 §6대로 표 3 재현 근거와 R1-1·R1-11의 소형 셀 오염 증거로 쓴다.
- 데스크톱 CPU 27h 재측정은 **하지 않는다**. 기기가 불안정해 재측정해도 더 나쁜 데이터를 얻는다.

**그래도 재측정을 하려면** 관리자 권한으로 (1) NVIDIA 앱의 오버레이(게임 내 오버레이) 비활성화,
(2) PresentMon·FvContainer·NexonLauncher 종료 및 시작프로그램 해제, (3) 가능하면 3월 당시 드라이버로
롤백한 뒤, 이 프로브를 다시 돌려 v1 대비 중앙값이 1.05 이내로 들어오는지 먼저 확인한다.
프로브 명령은 `paper/61_remeasure_runbook.md` 8절에 있다.

진단 데이터: `results/diag_20260915_cuda_small.json`(ANN 2·CNN 1 × 5회),
`results/diag_20260915_cpu_small.json`(ANN 3·CNN 1 × 5회), 클럭 샘플 `results/diag_20260915_gpu_clock.csv`.

## 8. 2026-09-16 분석 결과 (v4k) — 최종 수치와 원고 수정 지시

소스: `paper/v4_measured_k.json`, 표 초안: `paper/v4k_tables.md` (`make_tables_v4k.py`). 모든 값은 데스크톱
스키마 누수 수정 후(§8.0) 기준. 별도 표기가 없으면 구성 단위 5-겹 nested CV(내부 4-겹).

### 8.0 분석 중 발견한 데이터 결함과 수정

데스크톱 결과 파일은 구버전 추출기 산출이라 `input_height/width/channels`, `num_classes`, `hidden_size`,
`num_filters`, `embed_dim`, `num_heads`, `patch_size`, `latent_dim`, `generator/discriminator_params` 등
구성에서 결정되는 구조 필드 24개가 없었다(→ 0). 맥 행에는 있어서 이 열들이 "데스크톱=0 / 맥≠0"으로
**기기를 식별하는 누수**로 작동했다. `load_clean()`에서 같은 구성의 맥 행 값으로 채웠다(구조 필드는 구성만의
함수, 하드웨어·측정 필드는 복사 금지). 수정 후 백엔드 간 비-하드웨어 열 불일치 0.

| 수치 | 수정 전 | 수정 후 | 비고 |
|---|---:|---:|---|
| 전체 R²(log) 학습/추론, 구성 단위 | 0.9904 / 0.9891 | **0.9906 / 0.9891** | 논문 0.991/0.990 유지 (추론은 0.989로 반올림됨) |
| 무작위 분할 XGBoost (표 5) | 0.9884 / 0.9891 | **0.9852 / 0.9862** | **표 5 수치 갱신 필요** (0.988→0.985, 0.989→0.986) |
| 하드웨어 30개 제거 | 0.784 | 0.723 | 누수가 사라져 정직한 값으로 |
| 제거 + 장치 종류만(3수준) | 0.990 | 0.948 | 두 CPU를 못 가르면 떨어지는 게 정상 |

수정 전 결과는 `paper/v4_measured_k_before_schema_fix.json`에 보관. 핵심 주장 수치는 하드웨어 피처가 이미
백엔드를 알려주고 있었기 때문에 변하지 않았다. §3.1 또는 데이터 절에 이 수정을 한 문장으로 명시한다.

### 8.1 지적별 최종 답

**R1-1 R²(log)와 체감 정확도.** 구성 단위 OOF 기준:

| 타깃 | 구간 | n | MAPE | MdAPE | ±20% 이내 |
|---|---|---:|---:|---:|---:|
| 학습 | 전체 | 632 | 17.7% | 14.1% | 66.3% |
| 추론 | 전체 | 632 | 43.4% | 16.4% | 58.9% |
| 추론 | ≥10 ms | 515 | 19.5% | 14.0% | 68.7% |
| 추론 | <10 ms | 117 | 148.5% | 90.1% | 15.4% |

<10 ms 구간에 **오버헤드 피처**(op 수 × 배치 수 등 5개)를 더해도 개선 없음(148→168%), Huber 목적함수는 소폭
(→120%). 그러나 **소형 전문 모델**(추론 <50 ms 184셀만으로 학습·검증, 구성 단위)은 같은 117셀에서
**MAPE 22.6%, MdAPE 18.6%, ±20% 이내 55.6%**. 학습시간도 <1 s 166셀 전문 모델이 MAPE 17.5%→8.1%.
→ 소형 구간 오차는 측정 잡음도, 예측 불가능한 것도 아니라 **통합 로그 목적함수가 소형 셀을 과소 가중**한 결과.
원고: 초록·§4.1에 MAPE·MdAPE·±20% 병기, "매우 높다" 삭제, 전문 모델 결과를 §4.1 끝에 한 문단(또는 표 1행).

**R1-2 분할 논증.** 시드 10개: 무작위 0.9845±0.0012 / 0.9848±0.0017, 구성 0.9882±0.0013 / 0.9874±0.0010
(학습/추론). 짝지은 차이 +0.0037±0.0018, +0.0026±0.0018로 10/10 시드에서 구성 단위가 높다.
원인 조사: (i) 하이퍼파라미터 선택 아님 — 시드 0에서 두 프로토콜이 동일 파라미터(400, 3)를 고르고도 차이,
내부 루프만 구성 단위로 바꾼 교차 실험도 무작위 점수 불변. (ii) 외부 분할의 **학습 집합 구조** — 구성 단위는
학습 집합의 모든 구성이 네 백엔드를 완비, 무작위 80% 추출은 네 백엔드가 다 남는 구성이 0.8⁴=41%뿐이라
백엔드 효과 학습에 불리. 무작위의 누수 이점보다 이 불리함이 조금 더 크다.
→ 리뷰어 전제("엄격하면 낮아야")는 두 프로토콜의 차이가 누수뿐일 때만 성립. 어느 쪽이든 차이는 R²의 0.4%.
**진짜 구속 조건은 사다리 3단계**(§8.2). 원고: §4.2를 사다리 표로 교체하고 이 문단으로 서술.

**R1-3 구분 피처.** 30개 중 실제 값이 있는 것은 6개(`device_type_encoded`, `cpu_cores`, `ram_total_gb`,
`gpu_cores`, `gpu_memory_gb`, `cpu_freq_ghz`). 24개(`tflops_fp32`, `peak_bandwidth_gbs` 등)는 전 행 0.
Mac CPU↔MPS는 `device_type_encoded`·`gpu_cores`·`gpu_memory_gb`, Desktop CPU↔Mac CPU는 `cpu_cores`·
`ram_total_gb`·`cpu_freq_ghz`로 갈린다. 원고: §3.3에 이 문장, 표 4의 "하드웨어 36열"에 "수집된 값이 있는
열은 6개" 명시.

**R1-4 LOFO.** 사다리 6단계로 복원(내부 튜닝 포함, 계열 식별자 제외). 계열별 R²(log): 학습 CNN 0.87, ResNet
0.84, MobileNet 0.58, ViT 0.55, ANN·GAN 음수; 추론 ResNet 0.96, CNN 0.88, MobileNet 0.88, ViT 0.72, ANN·GAN 음수.
원고: 영문본에 §4.4 없으면 사다리 표 + 이 문장으로 복원, 서론 기여에 NAS 시나리오 검증 명시.

**R1-5 하드웨어 피처의 의미.** (a) 전체 0.9906/0.9891, (b) 30개 제거 0.7229/0.6323, **(c) 제거 + 백엔드 라벨
1개 0.9892/0.9885**, (d) 제거 + 장치 종류만 0.9476/0.9628, (e) 원-핫 0.9915/0.9882.
→ 리뷰어 말대로 **라벨 1개가 30개 기술자와 같다.** 미지 백엔드(LOBO)에서 기술자 모델이 "나머지 세 백엔드
실측 평균" 기준선을 이기는 경우는 8개 중 5개(Mac CPU 학습·추론, Desktop CPU 추론, MPS 추론, …), Desktop
CPU 학습과 MPS 학습은 기준선이 이김, CUDA는 모두 실패.
원고: §4.2 "crucial" → "이 데이터셋에서 하드웨어 피처는 백엔드 정체성으로 작동하며(표 7 (c)행), 코어 수·
메모리가 정체성 이상의 전이 가능한 정보를 담는다는 증거는 약하다(LOBO 기준선 비교)". 서론 기여 1도 같은 톤.

**R1-6 중요도 출처 모델.** 통합 XGBoost 폴드별 permutation: `total_op_memory_write` Δ 0.565/0.843 대 `flops`
0.012/0.019; 메모리 그룹(3열) 0.625/0.939 대 FLOPs 그룹(14열) 0.192/0.047; 하드웨어 30개 0.532/0.694;
계열 식별자 0.018/0.005. gain 순위 mem_write **5폴드 모두 1위**(학습·추론). 원고: 표 8·9를 이것으로 교체,
RF 백엔드별은 "동일 순위" 한 줄, 서론 기여 3을 통합 모델 기준으로 정정.

**R1-7 프록시의 고유성.** 대칭 절제: 전체 0.9906/0.9891, −메모리 0.9844/0.9828, −FLOPs 0.9892/0.9901, −둘 다
0.9854/0.9837 → 어느 그룹도 고유하지 않다(상관 0.88, 계열 내 0.95~0.996). 단독 피처: mem_write 0.703/0.598,
flops 0.556/0.487, params 0.202/0.308; **+백엔드 라벨: mem_write 0.940/0.940, flops 0.786/0.796**.
백엔드 내 타깃과의 Spearman: mem_write 0.89~0.97, flops 0.84~0.92.
→ 주장의 정확한 형태: "메모리 쓰기 프록시는 **가장 좋은 단일 정적 기술자**다(라벨과 둘이서 R² 0.94).
고유 정보는 아니며 제거해도 상관 피처가 대체한다." 배치 1 계산 한계는 §3.3에 명시(배치 고정이므로 샘플당
활성화 부피 기술자). 원고: §4.3 제목 "우위"→"일관성", 표 9를 대칭 절제+단독 기준선으로.

**R1-8 튜닝 공정성.** 동일 그리드 nested CV(무작위 분할): GB(grid) 0.9851/0.9865 ≈ XGB(grid) 0.9852/0.9862; 구성 단위: GB 0.9891/0.9904 ≈ XGB 0.9906/0.9891 (타깃별로 한 번씩 앞섬),
GB(default) 0.9747/0.9751, RF(grid) 0.9811/0.9763, Ridge 0.849/0.787, LR 0.844/0.777. 구성 단위에서 정규화 없는 LR은 −2.05로 발산하므로 표 5의 선형 기준선은 Ridge로 교체.
→ 리뷰어 지적 그대로 **0.988 대 0.977은 튜닝 차이**였고 GB와 XGB는 동률. 원고: 표 5 수치 교체, §3.4 "트리
앙상블 세 모델은 같은 그리드" 명시, "속도 때문에 XGBoost 채택". 구성 단위 버전도 `v4k_tables.md`에 있음.

**R1-9 표 값 일치.** 무작위 분할 4자리: 전체 대 Desktop CPU 학습 0.9884 대 0.9876(수정 전 값 기준), 추론은
4자리까지 같지만 부트스트랩 CI가 별개. 원고: 표 6을 4자리·CI로, 각주 "부분집합 자체 평균 기준, 분해 아님".

**R1-10 GAN 워밍업.** 두 플랫폼 재측정(§5·§6)에서 워밍업 없던 GAN의 변화가 워밍업 있던 계열과 동일 →
측정값에 영향 없음. 원고: §3.1에 워밍업 서술을 현행대로 정확히 쓰고 각주로 재측정 결과.

**R1-11 측정 잡음.** 셀별 10회 CV 중앙값: 학습 1.0%(p90 5.0%), 추론 1.5%(p90 14.6%), 추론 <10 ms 8.5%
(p90 29.4%). 백엔드별 학습 CV: Desktop CPU 1.2, CUDA 2.1, Mac CPU 0.7, MPS 0.5%. 예측 상대오차 중앙값은
학습 14.1%, 추론 16.4%, <10 ms 90.1% → **예측 오차가 측정 잡음의 10~13배**, 예측이 2σ 안에 드는 셀은 13~16%.
원고: §3.1에 CV 요약 한 문장, §4.1 오차 표에 "잡음 CV" 열(`v4k_tables.md` 오차 표).

**R1-12 환경.** 데스크톱 Python 3.11.9 · PyTorch 2.7.1+cu118 · CUDA 11.8 · cuDNN 9.1.0 · oneDNN 3.7.1 ·
Windows 11 26200 · 드라이버 610.62(측정 후 확인), 맥 Python 3.13.5 · PyTorch 2.10.0 · macOS 26.6.2 · Accelerate.
원고: §3.1에 기재. 데스크톱은 "측정 후 동일 환경에서 확인" 명시.

**R2.** 재현성은 표 3 재현(§6)으로, 미지 하드웨어는 LOBO 기준선 비교(R1-5)로, 고정 배치·피크 메모리는
Limitations 소절로. 배치 스윕·외부 5번째 백엔드는 데스크톱 불안정(§7)으로 보류.

### 8.2 검증 프로토콜 사다리 (표 7 교체안)

| 단계 | 홀드아웃 단위 | 학습 R²(log) | 학습 MAPE | 추론 R²(log) | 추론 MAPE(≥10ms) |
|---|---|---:|---:|---:|---:|
| 1 | 무작위 (셀), 시드 10 | 0.985 ± 0.001 | 20.1 ± 1.0% | 0.985 ± 0.002 | 24.1 ± 1.3% |
| 2 | 구성 (4백엔드 묶음), 시드 10 | 0.988 ± 0.001 | 18.8 ± 0.9% | 0.987 ± 0.001 | 21.9 ± 1.0% |
| 3 | 폭 수준 전체 (계열별 한 폭 값) | 0.955 | 39.8% | 0.959 | 72.5% |
| 4 | 깊이 수준 전체 | 0.975 | 30.5% | 0.983 | 37.2% |
| 5 | 계열별 최대 사분위 (161셀 외삽) | 0.912 | 66.2% | 0.882 | 152.8% |
| 6 | 계열 전체 (LOFO) | 0.783 (계열별 −20.9~0.87) | 230% | 0.892 (−397~0.96) | 221% |
| 7 | 백엔드 전체 (LOBO) | 0.638 (백엔드별 −3.2~0.85) | 203% | 0.552 (−17.2~0.89) | 240% |

읽기: 1·2단계는 보간(격자 안), **3단계에서 처음 꺾인다**(격자 밖 폭). 5단계는 NAS의 "더 큰 후보" 질문에
해당하며 큰 구성을 **체계적으로 과소 추정**(예측/실측 중앙값 0.77 학습, 0.91 추론). 6·7단계는 붕괴.
6·7단계의 합산 R²(log)가 0.6~0.9로 보이는 것은 계열·백엔드 간 분산이 지배하기 때문이며(R1-1의 지적 그대로)
그룹별 값과 MAPE가 실상이다.

### 8.3 원고 수정 위치 (최종)

- **초록**: "R²(log) 0.991·0.989, MAPE 학습 17.7%·추론(≥10 ms) 19.5%, 10 ms 미만 소형 셀은 MdAPE 90%로
  통합 모델의 한계이나 소형 전문 모델로 19%까지 낮아짐" 수준으로. 메모리 프록시는 "가장 일관된 단일 기술자".
- **§1 기여**: 기여 1 "하드웨어를 피처로 인코딩" → "백엔드를 피처로 인코딩(하드웨어 기술자는 정체성으로 작동)";
  기여 2에 사다리·LOFO 명시; 기여 3 "통합 XGBoost의 permutation·gain 중요도로 검증".
- **§3.1**: 워밍업 정확 기술 + 재측정 각주(R1-10), CV 요약(R1-11), 두 기기 환경(R1-12), 스키마 정합 한 문장(§8.0).
- **§3.3**: 구분 피처 6개(R1-3), 프록시 배치 1 한계(R1-7).
- **§3.4**: 트리 앙상블 세 모델 동일 그리드(R1-8).
- **§3.5**: 사다리 7단계 정의로 교체.
- **§4.1**: 표 5 수치 교체(R1-8), 표 6을 4자리·CI로(R1-9), 오차 표 신설(R1-1·11), 전문 모델 문단(R1-1).
- **§4.2**: 표 7을 사다리로 교체, 라벨 대 기술자 행(R1-5), 논증 재작성(R1-2).
- **§4.3**: 표 8·9를 통합 XGBoost 중요도 + 대칭 절제 + 단독 기준선으로, 제목 "일관성"(R1-6·7).
- **§4.4**: LOBO에 기준선 비교 추가(R1-5), 표 3 재현 문장(§6, R2).
- **Limitations 소절 신설**: 고정 배치, 피크 메모리, 프로토타입 규모, 소형 구간 통합 모델 한계, 미지 백엔드 약한 전이, 측정 환경 드리프트(§5·§7).
- **§3.2**: "ANN_h16_l1 약 3.55배" → 재측정 기준 2.79배(§6) — v1 유지 시 그대로 두고 각주.
